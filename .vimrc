" enable basic syntax highlight
syntax on
" show line numbers in the left of file
set number
" enable a line behind current cursor(horizontal)
set cursorline
" enable a line after current cursor(vertical)
set cursorcolumn
" enale incremental search
set incsearch
" set tab = 4 space
set tabstop=4
" set file encoding/decoding to utf-8
set encoding=utf-8
" enable highligh for matching pairs of parentheses, brackets, and braces
set showmatch
" highlight all match candidates when searching
set hlsearch
" vim will not wrap lines, even if current line is too long for current screen
set nowrap
" optional setting below
" ignore cases during searching
"set ignorecase
" override ignorecase if pattern begin with captial letter
"set smartcase

